{
  "name": "watch-together-chrome",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy"
  },
  "dependencies": {
    "convex": "^1.33.1"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
